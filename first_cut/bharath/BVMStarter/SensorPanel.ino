//Sensors
#include "bad.h"

void  readSensors(void)
{
  
}

float getSensorValue(int i) //instantaneous value
{
	return 0;
}

float getLastStrokeValue(int i) // Useful for values like tidal volume delivered etc, which are aggregated over time or sensed at stages, like plateau pressure.
{
  return 0;
}
